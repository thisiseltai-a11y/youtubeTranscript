# Sprites

Drop reference images here (car sheets, house photos, anything for The Lot's
pixel art) — from this GitHub page, **Add file → Upload files**, or just
drag an image in.

Once an image is used to build/update a sprite, it's usually fine to delete
it again from this folder — nothing in the app reads from here at runtime,
it's just a handoff spot.
